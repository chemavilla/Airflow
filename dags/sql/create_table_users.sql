CREATE TABLE IF NOT EXISTS users
    (uuid       VARCHAR(255),
    name        VARCHAR(255),
    idname      VARCHAR(255),
    city        VARCHAR(255),
    state       VARCHAR(255),
    gender      VARCHAR(10),
    email       VARCHAR(255),
    phone       VARCHAR(255))